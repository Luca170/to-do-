<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link rel="stylesheet" href="/home.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <div id="todos">
        <% todos.forEach(todo=>{ %>
            <div class="todo">
            <form action="/delete" method="POST">
            <input type="text" readonly value="<%= todo.content %>" id="todo-content" name="content" autocomplete="off">
            <button class="submit" type="submit">Delete</button>
            </form>
            <form action="/edit" method="GET">
            <input type="hidden" value="<%= todo._id %>" name="id">
            <button class="submit" type="submit">Edit</button>
            </form>
            </div>
       <% }) %> 
        </div>
    <form id="add" action="/add" method="POST">
    <input type="text" id="input-add" name="content" autocomplete="off">
    <button type="submit" id="button-add">Add</button>
    </form>


</body>
</html>